'use client';
import styled from 'styled-components';

const Home = () => {
	return (
		<Container>
			<h1>Home</h1>
			<p>Content goes here</p>
		</Container>
	);
};

const Container = styled.main`
	padding: 4rem 0 0;
`;

export default Home;
